hstack
	text "| "
	for n in routes
		if current == n
			text [foreground: #ffaa46] n 
		else
			text n
		text " | "
	for n in routes
		// this should show home and login, but only shows Home
		@nav_item
			$name_slot
				text n
